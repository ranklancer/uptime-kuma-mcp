# Pre-commit secret scanning — rebhahnomicon homelab
#
# One-time setup (per clone):
#   pip install pre-commit
#   pre-commit install
#
# Manual full scan:
#   pre-commit run --all-files

repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
